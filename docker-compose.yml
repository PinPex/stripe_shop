services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=stripe_shop.settings
    volumes:
      - .:/app
    command: gunicorn stripe_shop.wsgi:application --bind 0.0.0.0:8000